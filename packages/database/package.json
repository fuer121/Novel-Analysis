{
  "name": "@novel-analysis/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "migrate": "tsc -p tsconfig.json --noEmit false --rootDir src --outDir dist && node dist/migrate.js",
    "test": "vitest run --root ../.. packages/database/src",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "kysely": "0.29.4",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@types/pg": "8.20.0"
  }
}
